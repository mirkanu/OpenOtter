---
plan: 01.7-01
phase: 01.7-hetzner-deployment
status: complete
self_check: PASSED
key-files:
  created: []
  modified:
    - src/app/page.tsx
---

## Summary

Cloudflare Access protection for `otter.gsdlabs.dev` is live and verified.

## What was built

Configured a Cloudflare Access self-hosted application (`OpenOtter`) via the Cloudflare API (account `04bc84539b1073de92780f3c7568d273`) with an Allow policy restricted to `manuelkuhs@gmail.com`. No dashboard interaction was required — the entire setup was automated using `CLOUDFLARE_API_KEY` + `CLOUDFLARE_EMAIL` from `/home/services/.env.production`.

Also fixed a bug uncovered by the deployment: `src/app/page.tsx` was fetching recordings via a loopback HTTP request to `NEXT_PUBLIC_APP_URL` (the public domain), which Cloudflare Access blocked on the server side (no auth cookie). Replaced with a direct `getAllRecordings()` DB call and added `export const dynamic = "force-dynamic"`.

## Verification

- Automated: `curl` to `https://otter.gsdlabs.dev` returns `302 → Cloudflare Access login` (BLOCKED_OK)
- Human: `manuelkuhs@gmail.com` logged in via One-time PIN and reached the recordings page
- Human: full upload → transcribe → list → play flow confirmed working behind the gate

## Deviations

- Task 1 was specified as a dashboard checkpoint, but the Cloudflare API supports full programmatic setup — completed via API instead, no user dashboard interaction needed.
- Bonus fix: recordings loopback fetch bug (would have silently broken after Access was enabled regardless of this phase).

## Self-Check: PASSED
