---
phase: 01.7-hetzner-deployment
verified: 2026-06-11T21:30:00Z
status: human_needed
score: 3/5 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Log in to https://otter.gsdlabs.dev with manuelkuhs@gmail.com via One-time PIN"
    expected: "Cloudflare Access login screen appears, OTP is sent to manuelkuhs@gmail.com, after code entry the OpenOtter recordings page loads"
    why_human: "Requires receiving a one-time PIN by email — Claude cannot read the user's inbox"
  - test: "Run the full upload -> transcribe -> list -> play flow behind the Access gate"
    expected: "Upload an audio file, wait for transcription, confirm the recording appears in the list, open it and confirm the audio player and transcript both display"
    why_human: "Requires authenticated browser session and file system interaction that cannot be automated without valid session cookies"
---

# Phase 1.7: Hetzner Deployment Verification Report

**Phase Goal:** Protect the already-live OpenOtter app with Cloudflare Access so that only manuelkuhs@gmail.com can reach it, then verify both the access gate and the end-to-end app flow.
**Verified:** 2026-06-11T21:30:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| 1  | Unauthenticated requests to https://otter.gsdlabs.dev are blocked before reaching the app | VERIFIED | `curl -L --max-redirs 0 https://otter.gsdlabs.dev` returns `302 https://snowy-bush-6abf.cloudflareaccess.com/cdn-cgi/access/login/otter.gsdlabs.dev?...` — unauthenticated traffic is redirected to the Cloudflare Access login, not the app |
| 2  | manuelkuhs@gmail.com can authenticate via Cloudflare Access and reach the app | UNCERTAIN | Cloudflare API confirms the "Allow Manuel" policy exists with `"email": "manuelkuhs@gmail.com"` and `"decision": "allow"`. App infrastructure is wired. Actual login requires a one-time PIN delivered to the user's inbox — cannot verify programmatically |
| 3  | After login, the full upload -> transcribe -> list -> play flow works end-to-end behind Access | UNCERTAIN | Cannot verify without an authenticated browser session. SUMMARY claims human verified this, but that claim cannot be cross-checked programmatically |
| 4  | ROADMAP SC: Next.js app deployed on Hetzner VPS via PM2 — already done prior to this phase | VERIFIED | STATE.md and SUMMARY both confirm this was done in a prior session. `page.tsx` has `export const dynamic = "force-dynamic"` and uses direct `getAllRecordings()` DB call — no loopback fetch that would break under Access |
| 5  | ROADMAP SC: Cloudflare Access limits access to authorized email | VERIFIED | Cloudflare API (`/access/apps/db44f6d0-5283-46b6-a95e-dc81bcfb1d72/policies`) returns policy named "Allow Manuel", `"decision": "allow"`, with include rule `"email": "manuelkuhs@gmail.com"` |

**Score:** 3/5 truths verified (2 require human confirmation)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| Cloudflare Access application "OpenOtter" | Self-hosted app for otter.gsdlabs.dev | VERIFIED | API confirms app id `db44f6d0-5283-46b6-a95e-dc81bcfb1d72`, name "OpenOtter", domain "otter.gsdlabs.dev" |
| Allow policy for manuelkuhs@gmail.com | Policy named "Allow Manuel" with emails-include selector | VERIFIED | API confirms policy with `"decision": "allow"` and `"email": "manuelkuhs@gmail.com"` |
| `src/app/page.tsx` loopback bug fix | Direct DB call instead of HTTP fetch, force-dynamic | VERIFIED | File uses `getAllRecordings()` directly and exports `dynamic = "force-dynamic"` — no HTTP fetch that would be blocked by Access |

This phase has no code artifacts to create beyond the bug fix noted in the SUMMARY (which is confirmed by reading `src/app/page.tsx`).

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| Cloudflare Access application | otter.gsdlabs.dev | self-hosted application domain match | VERIFIED | API confirms `"domain": "otter.gsdlabs.dev"` on the OpenOtter Access app |
| Access policy (Allow) | manuelkuhs@gmail.com | Emails include selector | VERIFIED | API response includes `"email": "manuelkuhs@gmail.com"` in the include rule with `"decision": "allow"` |

### Data-Flow Trace (Level 4)

Phase 1.7 is infrastructure configuration, not a data-rendering artifact phase. Level 4 data-flow trace is not applicable. The `page.tsx` bug fix verified separately: `getAllRecordings()` queries SQLite directly (confirmed in `recording-store.ts:48`) — no static return, real DB data flows to the component.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Unauthenticated access blocked | `curl -s -o /dev/null -w "%{http_code} %{redirect_url}" -L --max-redirs 0 https://otter.gsdlabs.dev` | `302 https://snowy-bush-6abf.cloudflareaccess.com/cdn-cgi/access/login/otter.gsdlabs.dev?...` | PASS |
| Authenticated login + full flow | Requires live browser session with OTP | Cannot test programmatically | SKIP — human needed |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| UI-01 | 01.7-01-PLAN.md | Mobile-responsive interface works in Safari on iOS | MISMATCH | REQUIREMENTS.md traceability table maps UI-01 to "Phase 5: Mobile UI" (Milestone 2), not Phase 1.7. Phase 1.7 implements Cloudflare Access (authentication gate), which does not satisfy UI-01 (mobile responsiveness). This appears to be a planning error — the PLAN frontmatter claimed UI-01 but the phase work does not implement or test mobile responsiveness. UI-01 remains PENDING per REQUIREMENTS.md and is correctly deferred to Phase 5. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `src/app/page.tsx` | 14-16 | Error variable captured but real message not logged server-side | Warning (from code review WR-01) | Production debugging difficulty — error string is captured in the variable but never passed to `console.error`. Identified in 01.7-REVIEW.md as WR-01. Does not block the phase goal. |

No STUB patterns found in `page.tsx`. The component makes a real DB call and renders dynamic data.

### Human Verification Required

#### 1. Authenticated login via Cloudflare Access

**Test:** Open https://otter.gsdlabs.dev in a fresh incognito/private browser window. When the Cloudflare Access login screen appears, enter manuelkuhs@gmail.com, request the one-time PIN, retrieve the code from the inbox, enter it, and confirm the OpenOtter recordings page loads.
**Expected:** Cloudflare Access login screen appears immediately. After entering the correct OTP, the recordings page (or empty state if no recordings exist) loads — not a 403 or error page.
**Why human:** Requires receiving a one-time PIN delivered to manuelkuhs@gmail.com — Claude cannot read the user's email inbox.

#### 2. End-to-end flow behind the Access gate

**Test:** While logged in behind Access, upload an audio file (MP3/WAV/M4A) from the upload screen. Wait for transcription to complete. Return to the recordings list and confirm the new recording appears. Open the recording and confirm the audio player plays and the transcript with speaker labels displays.
**Expected:** Upload confirms success, transcription completes (may take 1-5 minutes via AssemblyAI), recording appears in the list, transcript and audio player are functional.
**Why human:** Requires an authenticated browser session and end-to-end interaction that cannot be automated without valid Access session cookies.

### Gaps Summary

No hard blockers. The three programmatically verifiable deliverables are all confirmed:

1. Cloudflare Access app "OpenOtter" exists for otter.gsdlabs.dev (API-verified)
2. Allow policy for manuelkuhs@gmail.com is wired with `"decision": "allow"` (API-verified)
3. Unauthenticated curl returns 302 to Cloudflare Access login, not app content (live-verified)
4. The loopback fetch bug in `page.tsx` is fixed — `getAllRecordings()` used directly (code-verified)

Two must-haves (authenticated login UX and full end-to-end flow) require human confirmation. These were flagged as human-verify checkpoints in the original PLAN (Task 2, type `checkpoint:human-verify`). The SUMMARY claims human completed these steps, but verification cannot independently confirm that.

**Requirement mismatch note:** UI-01 is claimed in the PLAN frontmatter but the REQUIREMENTS.md traceability table maps UI-01 to Phase 5. The work done in Phase 1.7 (Cloudflare Access setup) does not satisfy UI-01 (mobile responsiveness on Safari iOS). This is a bookkeeping issue — UI-01 remains deferred to Phase 5 as intended and is not a gap that blocks this phase's goal.

---

_Verified: 2026-06-11T21:30:00Z_
_Verifier: Claude (gsd-verifier)_
